[project]
name = "nomnom-plugin-rules"
version = "0.1.0"
description = "nomnom plugin: rules"
requires-python = ">=3.11"
dependencies = []

[project.entry-points."nomnom.plugins"]
rules = "nomnom_plugin_rules:RulesPlugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
